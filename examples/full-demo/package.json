{
  "name": "osx-ui-full-demo",
  "private": true,
  "type": "module",
  "scripts": {
    "dev": "npm run build:app && tsx server.ts",
    "build:app": "esbuild app.ts --bundle --outfile=app.js --format=esm --target=es2022",
    "start": "node server.js"
  },
  "dependencies": {
    "express": "^5.0.0"
  },
  "devDependencies": {
    "@types/node": "^25.3.0",
    "esbuild": "^0.27.3",
    "tsx": "^4.21.0"
  }
}
